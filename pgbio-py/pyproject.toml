[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "pgbio"
version = "0.1.0"
description = "Python SDK for pg_bio PostgreSQL extension"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "psycopg>=3.1",
]
